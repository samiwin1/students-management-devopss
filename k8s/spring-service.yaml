apiVersion: v1
kind: Service
metadata:
  name: students-management-svc
  namespace: dev

spec:
  type: NodePort
  selector:
    app: students-management
  ports:
    - port: 8089
      targetPort: 8089
      nodePort: 30089
